# Main-context overrides for non-root container execution.
# Included at the top of nginx.conf via Dockerfile sed injection.
# Only main-context directives belong here (pid, error_log).

pid /tmp/nginx/nginx.pid;
error_log /tmp/nginx/error.log warn;
